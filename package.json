{
  "name": "@jhorlin/agent-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental local-first, conflict-aware bridge for AI agent configuration",
  "type": "module",
  "bin": { "agent-bridge": "bin/agent-bridge.js" },
  "scripts": { "test": "node --test", "check": "node --check src/bridge.js && node --check bin/agent-bridge.js" },
  "engines": { "node": ">=22" }
}
